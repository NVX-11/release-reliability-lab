# README image assets

This directory is reserved for the final portfolio visuals referenced by the root README.

Expected files:

- `release-reliability-architecture.png` — final project architecture diagram.
- `promotion-proof.png` — verified promotion proof for 0.1.0 → 0.1.1.
- `rollback-proof.png` — controlled post-promotion failure and verified rollback proof.
- `availability-proof.png` — independent healthy → outage → recovery evidence.

The root README currently contains commented image slots so it renders cleanly before these files are added. When each image is ready, add it here using the exact filename and replace the matching comment in `README.md` with the prepared `<img>` block.
